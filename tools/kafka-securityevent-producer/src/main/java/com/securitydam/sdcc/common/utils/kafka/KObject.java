package com.securitydam.sdcc.common.utils.kafka;

/**
 * Stub matching sdcc-services KObject.
 * Must stay empty — Kryo FieldSerializer walks the full class hierarchy
 * and any field added here would shift the serialized layout.
 */
public abstract class KObject
{
}
