package com.securitydam.sdcc.common.utils.kafka;

/**
 * Stub matching sdcc-services KValue.
 * No additional fields — KValue adds only methods in the original.
 */
public class KValue extends KProperties<KValue>
{
}
